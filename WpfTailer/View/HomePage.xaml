<views:MvxWpfView x:Class="WpfTailer.View.HomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:views="clr-namespace:MvvmCross.Platforms.Wpf.Views;assembly=MvvmCross.Platforms.Wpf"

      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfTailer.View"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800">
    <views:MvxWpfView.Resources>
        <ResourceDictionary Source="Templates/TabResourceDictionary.xaml"/>
    </views:MvxWpfView.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30px"/>
        </Grid.RowDefinitions>
        <TabControl ItemsSource="{Binding FileTabs}"  DisplayMemberPath="FileName" ContentTemplate="{StaticResource TabDataTemplate}" 
                    SelectedIndex="{Binding FileTabsCount, Mode=OneWay}"
                    
                    >
        </TabControl>
        <DockPanel LastChildFill="False"
                   Grid.Row="1"
                   >
            <Button Command="{Binding OpenFileDialogCommand}" 
                    Content="Open File"
                    DockPanel.Dock="Right"
                    />
        </DockPanel>
    </Grid>
</views:MvxWpfView>
